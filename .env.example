# PostgreSQL Connection String (Prisma Postgres / Supabase / Neon / Local Postgres)
DATABASE_URL="postgres://postgres:postgres@localhost:51214/template1?sslmode=disable"

# Secret used to sign session cookies (at least 32 random characters)
SESSION_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"

# (Optional) Alpha Vantage API key for live real-time market quotes
# If not provided, PULSE automatically runs in graceful fallback mode using historical snapshots.
# MARKET_DATA_API_KEY="demo"
